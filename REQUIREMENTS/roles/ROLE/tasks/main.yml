---
- name: "install requirements.yml file (will always show as 'not changed')"
  community.general.ansible_galaxy_install:
    type: collection
    requirements_file: requirements.yml
  changed_when: false
